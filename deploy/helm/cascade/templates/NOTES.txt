Cascade control plane {{ .Chart.AppVersion }} has been deployed as release {{ .Release.Name }}.

The database migration ran as a pre-install/pre-upgrade hook (alembic upgrade head).

Check rollout status:
  kubectl -n {{ .Release.Namespace }} rollout status deployment/{{ include "cascade.fullname" . }}

{{- if .Values.ingress.enabled }}

The API is exposed through the ingress at:
{{- range .Values.ingress.hosts }}
  http{{ if $.Values.ingress.tls }}s{{ end }}://{{ .host }}
{{- end }}
{{- else }}

The service is ClusterIP only. Port-forward to reach it locally:
  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "cascade.fullname" . }} 8000:{{ .Values.service.port }}
  curl http://localhost:8000/livez
{{- end }}

Liveness and readiness are served at /livez and /readyz; Prometheus metrics at /metrics.
