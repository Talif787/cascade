apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "cascade.fullname" . }}-config
  labels:
    {{- include "cascade.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
